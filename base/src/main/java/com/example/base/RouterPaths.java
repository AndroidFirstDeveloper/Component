package com.example.base;

public interface RouterPaths {
    String LOGIN_ACTIVITY = "/login/login_activity";//group：login

    String LOGIN_PROVIDER = "/login/login_provider";//group：login (同一组件下group相同也可以)

    String SHARE_ACTIVITY = "/share/share_activity";
}
